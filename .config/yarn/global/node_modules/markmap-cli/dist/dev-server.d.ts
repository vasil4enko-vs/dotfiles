import { IDevelopOptions, IContentProvider } from './types';
export declare function develop(fileName: string | undefined, options: IDevelopOptions): Promise<{
    provider: IContentProvider;
    close(): Promise<void>;
}>;
